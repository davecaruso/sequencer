// Creative Toolkit - by dave caruso
// Actions that are frontend-compatible. These are run by the frontend as well as the backend,
// decreasing client latency. (such as changing a property on a resource)

export const _ = {}; // this line is needed to prevent a TS error
